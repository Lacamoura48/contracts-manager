---
deployment:
  tasks:
    - export DEPLOYPATH=/home/qosi2171/belfakih
    - export BUILDPATH=/home/qosi2171/belfakih/public/build
    - pwd
    - cd $BUILDPATH && rm -rf ./*
    - cd /home/qosi2171/contracts-manager
    - pwd
    - /bin/cp -r * $DEPLOYPATH #copy all from root

    #- /opt/alt/alt-nodejs16/root/usr/bin/npm install
    
